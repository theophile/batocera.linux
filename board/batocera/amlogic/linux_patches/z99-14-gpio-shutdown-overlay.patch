From ffb37d79f2b0f66f1c03a6a596f8b5d0bda41332 Mon Sep 17 00:00:00 2001
From: theophile <chmeredith@gmail.com>
Date: Wed, 5 Oct 2022 19:11:04 -0500
Subject: [PATCH] dt overlays: add gpio-shutdown overlay

---
 arch/arm64/boot/dts/amlogic/overlay/Makefile  |  3 +-
 .../overlay/meson-g12a-gpio-shutdown.dts      | 28 +++++++++++++++++++
 drivers/power/reset/gpio-poweroff.c           |  6 ----
 3 files changed, 30 insertions(+), 7 deletions(-)
 create mode 100644 arch/arm64/boot/dts/amlogic/overlay/meson-g12a-gpio-shutdown.dts

diff --git a/arch/arm64/boot/dts/amlogic/overlay/Makefile b/arch/arm64/boot/dts/amlogic/overlay/Makefile
index 45e47631ec9dd..f63d81950df3d 100644
--- a/arch/arm64/boot/dts/amlogic/overlay/Makefile
+++ b/arch/arm64/boot/dts/amlogic/overlay/Makefile
@@ -12,7 +12,8 @@ dtbo-$(CONFIG_ARCH_MESON) += \
 	meson-g12a-uart-ao-b-on-gpioao-8-gpioao-9.dtbo \
 	meson-g12a-uart-ee-c.dtbo \
 	meson-g12a-w1-gpioao-3.dtbo \
-	meson-g12a-gpio-poweroff.dtbo
+	meson-g12a-gpio-poweroff.dtbo \
+	meson-g12a-gpio-shutdown.dtbo
 
 scr-$(CONFIG_ARCH_MESON) += \
        meson-fixup.scr
diff --git a/arch/arm64/boot/dts/amlogic/overlay/meson-g12a-gpio-shutdown.dts b/arch/arm64/boot/dts/amlogic/overlay/meson-g12a-gpio-shutdown.dts
new file mode 100644
index 0000000000000..855cf119e5942
--- /dev/null
+++ b/arch/arm64/boot/dts/amlogic/overlay/meson-g12a-gpio-shutdown.dts
@@ -0,0 +1,28 @@
+/dts-v1/;
+/plugin/;
+
+#include <dt-bindings/input/input.h>
+#include <dt-bindings/gpio/gpio.h>
+#include <dt-bindings/gpio/meson-g12a-gpio.h>
+
+/ {
+        compatible = "radxa,zero", "amlogic,g12a";
+
+        fragment@0 {
+                target-path = "/";
+                __overlay__ {
+                        gpio-keys-polled {
+                                compatible = "gpio-keys-polled";
+                                poll-interval = <100>;
+
+                                power-button {
+                                        label = "power";
+                                        linux,code = <KEY_POWER>;
+                                        gpios = <&gpio_ao GPIOAO_4 (GPIO_ACTIVE_LOW)>;
+                                };
+                        };
+                };
+        };
+};
+
+
diff --git a/drivers/power/reset/gpio-poweroff.c b/drivers/power/reset/gpio-poweroff.c
index d05ddc58bd092..3b61f55526cb5 100644
--- a/drivers/power/reset/gpio-poweroff.c
+++ b/drivers/power/reset/gpio-poweroff.c
@@ -80,12 +80,6 @@ static int gpio_poweroff_probe(struct platform_device *pdev)
 	if (IS_ERR(reset_gpio))
 		return PTR_ERR(reset_gpio);
 
-	export = of_property_read_bool(pdev->dev.of_node, "export");
-	if (export) {
-		gpiod_export(reset_gpio, false);
-		gpiod_export_link(&pdev->dev, "poweroff-gpio", reset_gpio);
-	}
-
 	old_power_off = pm_power_off;
 	pm_power_off = &gpio_poweroff_do_poweroff;
 	return 0;
